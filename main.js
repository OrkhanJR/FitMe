const firstNumber = +prompt('Type the first number');
const secondNumber = +prompt('Type the second number');
const sumResult = ('The result is ' + (firstNumber + secondNumber));
console.log(sumResult);